# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
#default: &default
  #adapter: mysql2
  #pool: 5
  #timeout: 5000
  #encoding: utf8
  #database: soccermanager
  #username: root
  #password: california
  #host: 127.0.0.1
  #port:


development:
  adapter: mysql2
  encoding: utf8
  database: soccermanagaer_development
  pool: 5
  username: root
  password: california
  socket: /var/run/mysqld/mysqld.sock #Linux
  #socket: /tmp/mysql.sock #iOS


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
#test:
  #<<: *default
  #database: db/test.soccermanager

#production:
  #<<: *default
  #database: db/production.soccermanager
